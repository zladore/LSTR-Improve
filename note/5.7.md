Exp4-v8 inference-only text logit residual：
不训练新分支，直接在原始三模态 epoch-14 checkpoint 上加入极小 text residual。
其中 beta=0.005, temp=0.10, topk=3 的 raw BatchInference mAP 为 71.520，略高于原始三模态 raw 71.518。
说明 class text residual 在很小强度下不会破坏原始主路，并可能带来极小幅度修正。
下一步需要看同样设置下的 classifier-only TTA 是否能超过 71.950。


正在跑：
1. Exp4-v8 beta=0.005 temp=0.10 topk=3 + TTA

马上补：
2. Exp3-v1 Hard Negative Alignment，4 组并行
3. Exp1-v2 Soft Semantic Alignment，2 组并行

之后再做：
4. Top-k hit rate / GT rank 诊断脚本
5. 如果 v8 TTA 有效，做 beta/temp 小范围复现；

训练式 alignment，包括 hard negative 和 soft semantic，目前都不理想，不继续跑。
当前最有希望的是 inference-only text logit residual。
Exp4-v8 raw 已经做到 71.520，略高于原始 raw 71.518。
下一步关键看：
1. v8 residual 参与 TTA 是否超过 71.950；
2. v9 TTA 后再加 residual 是否超过 71.950。
