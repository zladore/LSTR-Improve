# LSTR-改进

假设LSTR原本训练推理mAP是70%，我先不动训练分支，我们的想法是认为在推理的时候增加loss使得模型可以更好的适应数据，也就是在推理时，原本模型给出概率，给出对应的类别A，那么我们已经有了该类别对应的类别特征，同时也有该帧对应的文本描述的特征，让这两个特征做损失，（比如相似度损失），进行训练使其收敛，在这个过程中我们可以去迭代修改这个类别A， 经过这样的过程最后出来的mAP 看看是否会好于在推理时未加此损失训练的mAP，
